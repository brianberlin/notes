name: Release
on:
  push:
    branches: [ master ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Elixir
        uses: actions/setup-elixir@v1
        with:
          elixir-version: "1.11.4"
          otp-version: "23.2.6"
      - name: Build
      - run: mix release